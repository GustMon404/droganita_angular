import { DescCompletaPipe } from './desc-completa.pipe';

describe('DescCompletaPipe', () => {
  it('create an instance', () => {
    const pipe = new DescCompletaPipe();
    expect(pipe).toBeTruthy();
  });
});
