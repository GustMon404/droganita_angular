import { ExpandirDirective } from './expandir.directive';

describe('ExpandirDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandirDirective();
    expect(directive).toBeTruthy();
  });
});
